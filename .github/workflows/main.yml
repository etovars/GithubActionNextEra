name: Hello World Workflow

on: [workflow_call]

jobs:
  say-hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Say Hello
        run: echo "Hello, world! by Manual"
